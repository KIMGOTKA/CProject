#include <stdio.h>

int main0() {
	
	int kor = 50, eng = 44, math = 60;
	int ff = 0 + 0 + 0;
	double bb = 0.0;
	//ÇÕ°è
	//Æò±Õ


	ff = kor + eng + math;

	printf("ÇÕ°è: %d\n", ff);

	bb = ff / 3.0;  // bb= (double)aa/3;

	printf("Æò±Õ: %lf", bb);


	return 0;
}
